add toasts in frontend
add checks in login and register along with frontend
fix generatetext ==> streamtext
fix how md responses are handled in ai responses
instead of sending whole messages array try and implement knowledge base method for each user 
apply limit so current chat holds only 20 messages
make a panel in ai so that it helps easy scrolling
add helmet and ratelimit on every api we do 
add more functionality to chat like clearchat/newchat, check context and clear context 

add ai guard rails

later : try turn ai into a sidepane instead

